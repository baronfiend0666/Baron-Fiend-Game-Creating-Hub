window.PORTFOLIO_DATA = {
  projects: [
    {
      id: "patris",
      order: "01",
      name: "Patris",
      nameKo: "파트리스",
      genre: "3인칭 오픈월드 캐릭터 수집형 액션 RPG",
      engine: "Unreal Engine 5",
      background: "assets/images/patris-project-visual.png",
      tags: ["3인칭", "오픈월드", "캐릭터 수집", "액션 RPG"],
      summary: "플레이어는 데미안 오펠 포네로스가 되어 파트리스의 5개 지역을 탐험하고, 동료 캐릭터들과 성장하며 세계를 위협하는 적들과 대립한다.",
      youtube: "",
      cardImage: "assets/images/patris-project-visual.png",
      appIcon: "assets/images/patris/app-icons/patris-app-icon-isadora.png",
      appIcons: [
        {
          label: "Isadora",
          title: "Patris App Icon - Isadora",
          path: "assets/images/patris/app-icons/patris-app-icon-isadora.png",
          fallbackPaths: [
            "assets/images/app-icons/patris-app-icon-isadora.png",
            "assets/images/patris-app-icon-isadora.png",
          ],
        },
        {
          label: "Iris",
          title: "Patris App Icon - Iris",
          path: "assets/images/patris/app-icons/patris-app-icon-iris.png",
          fallbackPaths: [
            "assets/images/app-icons/patris-app-icon-iris.png",
            "assets/images/patris-app-icon-iris.png",
          ],
        },
      ],
      videoTitle: "파트리스(Patris)",
      videoDescription: "AniFlow 사이트와 각 프로젝트의 스토리보드 문서를 기반으로 제작한 AI 애니메이션의 1화 영상 유튜브 URL입니다.",
      projectDisplayLine: "파트리스 · 3인칭 오픈월드 캐릭터 수집형 액션 RPG",
    },
    {
      id: "crimson-frequency",
      order: "02",
      name: "Crimson Frequency",
      nameKo: "크림슨 프리퀀시",
      genre: "3인칭 현대 로그라이트 액션 RPG",
      engine: "Unity",
      background: "assets/images/crimson-frequency-project-visual.png",
      tags: ["3인칭", "현대", "로그라이트", "액션 RPG"],
      summary: "플레이어는 정체를 숨긴 먼치킨 스트리머 헬 하운드가 되어 서울 한복판에 나타난 탑을 등반하고 자신을 죽이려 한 자들을 추적해 복수한다.",
      youtube: "",
      cardImage: "assets/images/crimson-frequency-project-visual.png",
      appIcon: "assets/images/crimson-frequency/app-icons/crimson-frequency-app-icon.png",
      appIcons: [
        {
          label: "Crimson Frequency",
          title: "Crimson Frequency App Icon",
          path: "assets/images/crimson-frequency/app-icons/crimson-frequency-app-icon.png",
          fallbackPaths: [
            "assets/images/app-icons/crimson-frequency-app-icon.png",
            "assets/images/crimson-frequency-app-icon.png",
          ],
        },
      ],
      videoTitle: "크림슨 프리퀀시(Crimson Frequency)",
      videoDescription: "AniFlow 사이트와 각 프로젝트의 스토리보드 문서를 기반으로 제작한 AI 애니메이션의 1화 영상 유튜브 URL입니다.",
      projectDisplayLine: "크림슨 프리퀀시 · 3인칭 현대 로그라이트 액션 RPG",
    },
    {
      id: "anomaly-record",
      order: "03",
      name: "Anomaly Record",
      nameKo: "아노말리 레코드",
      genre: "3인칭 오컬트 어반 판타지 RPG",
      engine: "Unity",
      background: "assets/images/anomaly-record-project-visual.png",
      tags: ["3인칭", "오컬트", "어반 판타지", "RPG"],
      summary: "플레이어는 한시우 또는 류세린을 선택해 괴이대응청 현장분석국 분석관으로서 서울 곳곳에 출현하는 괴이 사건을 해결한다.",
      youtube: "",
      cardImage: "assets/images/anomaly-record-project-visual.png",
      appIcon: "assets/images/anomaly-record/app-icons/anomaly-record-app-icon.png",
      appIcons: [
        {
          label: "Anomaly Record",
          title: "Anomaly Record App Icon",
          path: "assets/images/anomaly-record/app-icons/anomaly-record-app-icon.png",
          fallbackPaths: [
            "assets/images/app-icons/anomaly-record-app-icon.png",
            "assets/images/anomaly-record-app-icon.png",
          ],
        },
      ],
      videoTitle: "아노말리 레코드(Anomaly Record)",
      videoDescription: "AniFlow 사이트와 각 프로젝트의 스토리보드 문서를 기반으로 제작한 AI 애니메이션의 1화 영상 유튜브 URL입니다.",
      projectDisplayLine: "아노말리 레코드 · 3인칭 오컬트 어반 판타지 RPG",
    },
  ],

  portfolioSections: [
    {
      id: "pre-production",
      order: "01",
      title: "사전 기획 포트폴리오",
      items: [
        { title: "개발 제안서", ext: "docx", slug: "Development_Proposal", path: "assets/docs/common/coming-soon.pdf#page=1" },
        { title: "콘텐츠맵", ext: "svg", slug: "Content_Map", path: "assets/docs/common/coming-soon.pdf#page=1" },
        { title: "게임 소개서", ext: "docx", slug: "Game_Introduction_Document", path: "assets/docs/common/coming-soon.pdf#page=1" },
        { title: "GDD", ext: "docx", slug: "GDD", path: "assets/docs/common/coming-soon.pdf#page=1" },
      ],
    },
    {
      id: "concept",
      order: "02",
      title: "콘셉트 기획 포트폴리오",
      items: [
        { title: "게임 스토리", ext: "docx", slug: "Game_Story", path: "assets/docs/common/coming-soon.pdf#page=1" },
        { title: "게임 시나리오", ext: "docx", slug: "Game_Scenario", path: "assets/docs/common/coming-soon.pdf#page=1" },
        { title: "스토리 보드", ext: "docx", slug: "Storyboard", path: "assets/docs/common/coming-soon.pdf#page=1" },
        { title: "캐릭터 컨셉 기획서", ext: "docx", slug: "Character_Concept_Document", path: "assets/docs/common/coming-soon.pdf#page=1" },
        { title: "배경 컨셉 기획서", ext: "docx", slug: "Background_Concept_Document", path: "assets/docs/common/coming-soon.pdf#page=1" },
        { title: "캐릭터 테이블", ext: "xlsx", slug: "Character_Table", path: "assets/docs/common/coming-soon.pdf#page=1" },
        { title: "아이템 테이블", ext: "xlsx", slug: "Item_Table", path: "assets/docs/common/coming-soon.pdf#page=1" },
        { title: "퀘스트 테이블", ext: "xlsx", slug: "Quest_Table", path: "assets/docs/common/coming-soon.pdf#page=1" },
        { title: "다이얼로그 테이블", ext: "xlsx", slug: "Dialogue_Table", path: "assets/docs/common/coming-soon.pdf#page=1" },
        { title: "시나리오 테이블", ext: "xlsx", slug: "Scenario_Table", path: "assets/docs/common/coming-soon.pdf#page=1" },
      ],
    },
    {
      id: "system",
      order: "03",
      title: "시스템 기획 포트폴리오",
      items: [
        { title: "게임 메커닉스", ext: "docx", slug: "Game_Mechanics", path: "assets/docs/common/coming-soon.pdf#page=1" },
        { title: "프로세스 플로우", ext: "svg", slug: "Process_Flow", path: "assets/docs/common/coming-soon.pdf#page=1" },
        { title: "ERD", ext: "svg", slug: "ERD", path: "assets/docs/common/coming-soon.pdf#page=1" },
        { title: "데이터 스키마", ext: "docx", slug: "Data_Schema", path: "assets/docs/common/coming-soon.pdf#page=1" },
        { title: "통합 데이터 테이블", ext: "docx", slug: "Integrated_Data_Table", path: "assets/docs/common/coming-soon.pdf#page=1" },
      ],
    },
  ],

  novelIPs: [
    {
      id: "patris-part-1",
      project: "Patris",
      label: "파트리스(Patris) (1부)",
      title: "강령명가의 악질분자로 살아남기",
      serialPeriod: "2026.07.10~",
      platforms: [
        { name: "문피아(Munpia)", url: "" },
        { name: "네이버 시리즈(Naver Series)", url: "" },
        { name: "카카오페이지(Kakaopage)", url: "" },
      ],
      titlePrefix: "원작 웹소설 IP",
    },
    {
      id: "patris-part-2",
      project: "Patris",
      label: "파트리스(Patris) (2부)",
      title: "합법적 성물 도둑",
      serialPeriod: "시기 미정",
      platforms: [
        { name: "문피아(Munpia)", url: "" },
        { name: "네이버 시리즈(Naver Series)", url: "" },
        { name: "카카오페이지(Kakaopage)", url: "" },
      ],
      titlePrefix: "원작 웹소설 IP",
    },
    {
      id: "crimson-frequency-novel",
      project: "Crimson Frequency",
      label: "크림슨 프리퀀시(Crimson Frequency)",
      title: "미스터 헬 하운드의 스트리밍",
      serialPeriod: "2027.08~",
      platforms: [
        { name: "문피아(Munpia)", url: "" },
        { name: "네이버 시리즈(Naver Series)", url: "" },
        { name: "카카오페이지(Kakaopage)", url: "" },
      ],
      titlePrefix: "원작 웹소설 IP",
    },
  ],
};

(() => {
  const STYLE_ID = "project-file-detail-app-icon-style";
  const ENHANCED_ATTR = "data-project-file-detail-app-icons";

  const data = window.PORTFOLIO_DATA ?? { projects: [] };

  const ensureStyles = () => {
    if (document.getElementById(STYLE_ID)) return;

    const style = document.createElement("style");
    style.id = STYLE_ID;
    style.textContent = `
      .project-file-detail-title-row,
      .project-detail-heading-wrap.project-file-detail-title-row {
        display: flex;
        align-items: center;
        gap: clamp(14px, 2vw, 20px);
        margin: 8px 0 10px;
      }

      .project-file-detail-title-row h3,
      .project-detail-heading-wrap.project-file-detail-title-row h3 {
        margin: 0;
      }

      .project-file-detail-app-icons {
        display: flex;
        align-items: center;
        gap: 10px;
        flex: 0 0 auto;
      }

      .project-file-detail-app-icons.is-single .project-file-detail-app-icon-img {
        width: 112px !important;
        height: 112px !important;
        border-radius: 28px !important;
      }

      .project-file-detail-app-icons.is-multiple .project-file-detail-app-icon-img {
        width: 96px !important;
        height: 96px !important;
        border-radius: 24px !important;
      }

      .project-file-detail-app-icon-img,
      .project-detail-app-icon.project-file-detail-app-icon-img {
        display: block !important;
        box-sizing: border-box !important;
        flex: 0 0 auto !important;
        max-width: none !important;
        max-height: none !important;
        aspect-ratio: 1 / 1 !important;
        object-fit: contain !important;
        object-position: center center !important;
        image-rendering: auto !important;
        background: rgba(7, 10, 22, 0.60);
        border: 1px solid rgba(255, 255, 255, 0.30);
        box-shadow:
          0 14px 30px rgba(0, 0, 0, 0.32),
          0 0 0 1px rgba(122, 162, 255, 0.10);
        transform: translateZ(0);
        backface-visibility: hidden;
      }

      .project-file-detail-app-icon-img.is-missing {
        opacity: 0.42;
      }

      @media (max-width: 720px) {
        .project-file-detail-title-row,
        .project-detail-heading-wrap.project-file-detail-title-row {
          align-items: flex-start;
          gap: 12px;
        }

        .project-file-detail-app-icons {
          gap: 8px;
        }

        .project-file-detail-app-icons.is-single .project-file-detail-app-icon-img {
          width: 88px !important;
          height: 88px !important;
          border-radius: 22px !important;
        }

        .project-file-detail-app-icons.is-multiple .project-file-detail-app-icon-img {
          width: 76px !important;
          height: 76px !important;
          border-radius: 20px !important;
        }
      }
    `;
    document.head.appendChild(style);
  };

  const normalize = (value) => String(value ?? "").trim().toLowerCase();

  const getVisibleProjectDetail = () => {
    const candidates = [...document.querySelectorAll("[data-project-detail-page]")];
    return candidates.find((detail) => !detail.hidden && detail.offsetParent !== null) || candidates.find((detail) => !detail.hidden) || null;
  };

  const getProjectIdFromActiveUI = () => {
    const params = new URLSearchParams(window.location.search);
    const fromUrl = params.get("project");
    if (fromUrl) return fromUrl;

    const activeFilter = document.querySelector('#documents [data-filter].is-active, #documents [aria-selected="true"][data-filter]');
    const fromFilter = activeFilter?.dataset?.filter;
    if (fromFilter && fromFilter !== "all") return fromFilter;

    const detail = getVisibleProjectDetail();
    const detailText = normalize(detail?.textContent);
    if (detailText) {
      const matched = data.projects.find((project) => {
        return detailText.includes(normalize(project.name)) || detailText.includes(normalize(project.nameKo));
      });
      if (matched) return matched.id;
    }

    return "";
  };

  const getCurrentProject = () => {
    const projectId = getProjectIdFromActiveUI();
    return data.projects.find((project) => project.id === projectId) || null;
  };

  const getProjectIcons = (project) => {
    if (!project) return [];
    if (Array.isArray(project.appIcons) && project.appIcons.length > 0) return project.appIcons;
    if (project.appIcon) return [{ title: `${project.name} App Icon`, label: project.name, path: project.appIcon }];
    return [];
  };

  const getProjectTitleElement = (detail, project) => {
    const directTitle = detail.querySelector(".project-detail-copy h3");
    if (directTitle) return directTitle;

    const headingList = [...detail.querySelectorAll("h2, h3")];
    return headingList.find((heading) => {
      const text = normalize(heading.textContent);
      return text.includes(normalize(project.name)) || text.includes(normalize(project.nameKo));
    }) || headingList[0] || null;
  };

  const makeIconStrip = (project, icons) => {
    const strip = document.createElement("span");
    strip.className = `project-file-detail-app-icons ${icons.length > 1 ? "is-multiple" : "is-single"}`;
    strip.setAttribute("aria-label", `${project.nameKo || project.name} 앱 아이콘`);

    icons.forEach((item, index) => {
      const img = document.createElement("img");
      img.className = "project-detail-app-icon project-file-detail-app-icon-img";
      const iconPaths = [
        item.path,
        ...(Array.isArray(item.fallbackPaths) ? item.fallbackPaths : []),
        item.fallbackPath,
      ].filter(Boolean).filter((path, pathIndex, array) => array.indexOf(path) === pathIndex);

      img.src = iconPaths[0] || "";
      img.alt = item.title || `${project.nameKo || project.name} 앱 아이콘 ${index + 1}`;
      img.loading = "eager";
      img.decoding = "async";
      img.width = icons.length > 1 ? 96 : 112;
      img.height = icons.length > 1 ? 96 : 112;
      img.dataset.iconPathIndex = "0";

      img.addEventListener("error", () => {
        const nextIndex = Number(img.dataset.iconPathIndex || "0") + 1;
        if (nextIndex < iconPaths.length) {
          img.dataset.iconPathIndex = String(nextIndex);
          img.src = iconPaths[nextIndex];
          return;
        }
        img.classList.add("is-missing");
        img.title = `${img.alt} 이미지 경로를 확인해야 합니다.`;
      });

      strip.appendChild(img);
    });

    return strip;
  };

  const enhanceProjectFileDetailTitle = () => {
    const detail = getVisibleProjectDetail();
    const project = getCurrentProject();
    const icons = getProjectIcons(project);

    if (!detail || !project || icons.length === 0) return;

    const title = getProjectTitleElement(detail, project);
    if (!title) return;

    const signature = `${project.id}|${icons.map((item) => item.path).join("|")}`;
    const currentRow = title.closest(".project-file-detail-title-row, .project-detail-heading-wrap");
    if (currentRow?.getAttribute(ENHANCED_ATTR) === signature) return;

    ensureStyles();

    let row = currentRow;
    if (!row) {
      row = document.createElement("div");
      title.parentNode.insertBefore(row, title);
      row.appendChild(title);
    }

    row.classList.add("project-detail-heading-wrap", "project-file-detail-title-row");
    row.querySelectorAll(".project-file-detail-app-icons").forEach((element) => element.remove());
    row.querySelectorAll(".project-detail-app-icon:not(.project-file-detail-app-icon-img)").forEach((element) => element.remove());

    row.insertBefore(makeIconStrip(project, icons), title);
    row.setAttribute(ENHANCED_ATTR, signature);
  };

  const scheduleEnhancement = () => {
    window.requestAnimationFrame(enhanceProjectFileDetailTitle);
    window.setTimeout(enhanceProjectFileDetailTitle, 120);
    window.setTimeout(enhanceProjectFileDetailTitle, 420);
  };

  const start = () => {
    scheduleEnhancement();

    window.addEventListener("popstate", scheduleEnhancement);
    window.addEventListener("hashchange", scheduleEnhancement);

    document.addEventListener("click", (event) => {
      const target = event.target instanceof Element ? event.target : null;
      if (!target) return;

      if (target.closest("#documents [data-filter], [data-project-image-link], [data-project-back-detail], [data-project-section-jump], .project-detail-index-card")) {
        scheduleEnhancement();
      }
    });

    const observer = new MutationObserver(scheduleEnhancement);
    observer.observe(document.body, { childList: true, subtree: true, attributes: true, attributeFilter: ["hidden", "class", "style"] });
  };

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", start, { once: true });
  } else {
    start();
  }
})();
